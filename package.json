{
  "name": "collaborative-task-manager",
  "version": "1.0.0",
  "description": "A task tracking and management application that facilitates collaboration within teams",
  "main": "src/app.js",
  "scripts": {
    "pretest": "node -e \"if (process.version.slice(1).split('.')[0] < 18) { console.error('Unsupported Node.js version, please use version 18'); process.exit(1); }\"",
    "test": "tap test/*.js --disable-coverage",
    "start": "nodemon app.js"
  },
  "author": "Airtribe",
  "license": "ISC",
  "devDependencies": {
    "jsonwebtoken": "^9.0.2",
    "nodemon": "^3.0.3",
    "supertest": "^6.3.4",
    "tap": "^18.6.1"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "mysql": "^2.18.1",
    "mysql2": "^3.9.7",
    "nodemon": "^3.1.0",
    "sequelize": "^6.37.3",
    "validator": "^13.11.0"
  },
  "engines": {
    "node": ">= 18.0.0"
  }
}
